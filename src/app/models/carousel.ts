export interface ProgramCarousel {
  title?: string;
  desc?: string;
  image?: object;
};

export interface GalleryCarousel {
  title?: string;
  desc?: string;
  image?: object;
};

export interface MitraCarousel {
  name?: string;
  logo?: object;
};

